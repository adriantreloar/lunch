class QueryParser:
    @staticmethod
    def parse(query):
        """Parse a query into an AST
        The AST will be combined with the Reference data (and formulae) to produce another AST
        That AST will be sent to an optimiser (along with statistics) to create query instructions for a given engine
        """
        return {}
